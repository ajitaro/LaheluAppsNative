import {View, Text} from 'react-native';
import React from 'react';
import globalStyles from '../../styles';

export default function CreatePost() {
  return (
    <View style={globalStyles.container}>
      <Text style={globalStyles.text}>CreatePost</Text>
    </View>
  );
}
